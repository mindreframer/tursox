defmodule Tursox.Database do
  @moduledoc """
  Opaque embedded database resource and the root of the direct API.

  A database derives many independent `Tursox.Connection` resources. Database
  behavior is introduced in roadmap Epic 2.
  """
end
