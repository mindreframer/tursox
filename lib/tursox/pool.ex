defmodule Tursox.Pool do
  @moduledoc """
  Optional `DBConnection` pool deriving workers from one database resource.

  Pool behavior is introduced in roadmap Epic 5; the direct API never starts a
  pool implicitly.
  """
end
